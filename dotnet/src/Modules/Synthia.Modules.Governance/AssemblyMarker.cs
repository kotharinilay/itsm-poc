namespace Synthia.Modules.Governance;

/// <summary>
/// Assembly marker. Architecture tests resolve this assembly by type reference rather than by
/// name string, so a project rename cannot silently disable the rule that guards it.
/// </summary>
public sealed class AssemblyMarker
{
    private AssemblyMarker()
    {
    }
}

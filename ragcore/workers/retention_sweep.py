"""Removes data past its retention window, per class.

Stage 1 stub: the entry point exists so the process boundary is real and the deployment
topology is honest. No behaviour is implemented here.
"""

from __future__ import annotations


def main() -> None:
    """Worker entry point. Implemented at the stage that gives this worker work to do."""
    raise NotImplementedError(
        "Not implemented at Stage 1 — see specs/001-platform-scaffold/tasks.md"
    )
